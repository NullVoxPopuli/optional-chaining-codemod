{
  "name": "@nullvoxpopuli/optional-chaining-codemod",
  "version": "0.2.1",
  "private": false,
  "license": "MIT",
  "scripts": {
    "lint": "eslint --cache .",
    "test": "codemod-cli test",
    "test:coverage": "codemod-cli test --coverage",
    "update-docs": "codemod-cli update-docs",
    "coveralls": "cat ./coverage/lcov.info | node node_modules/.bin/coveralls"
  },
  "bin": {
    "optional-chaining-codemod": "./bin/cli.js"
  },
  "keywords": [
    "codemod-cli"
  ],
  "dependencies": {
    "codemod-cli": "^2.1.0"
  },
  "devDependencies": {
    "coveralls": "^3.0.6",
    "eslint": "^6.5.1",
    "eslint-config-prettier": "^6.3.0",
    "eslint-plugin-node": "^10.0.0",
    "eslint-plugin-prettier": "^3.1.1",
    "jest": "^24.9.0",
    "prettier": "^1.18.2"
  },
  "engines": {
    "node": "8.* || 10.* || >= 12"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
